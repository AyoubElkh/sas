#include <stdio.h>

int main(){

    int a,b;

    printf("entre la valeur de a: ");
    scanf("%d",&a);
    printf("entre la valeur de b: ");
    scanf("%d",&b);

    if(a==b){
        printf("la resulta de %d", (a+b)*3);
    }
    else{
        printf("(la resulta de %d",(a+b));
    }

}
